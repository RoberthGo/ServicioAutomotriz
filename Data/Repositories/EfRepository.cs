// Entity Framework repository removed. Use the concrete SQL repositories in Data.Repositories (ServiceRepository, PartRepository) instead.
// This file left as a marker and to avoid build errors if referenced elsewhere.
namespace AutoRepair.Data.Repositories
{
    public class EfRepository<T> where T : class
    {
        // Intentionally left blank. Entity Framework support has been removed.
    }
}
